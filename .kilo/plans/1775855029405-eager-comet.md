# YouTube Shorts Builder - Fix Plan

## Issues to Fix

1. **404 Error on Download Route**
   - The `/download/<video_id>` route is missing from `simple_routes_fixed.py`
   - Need to add the route to serve completed videos

2. **BGM Directory Issue**
   - Using `assets/n8n_bgm` instead of `assets/retro_bgm`
   - Need to update to correct directory path

3. **Config File Issue**
   - Using `n8n_config.json` instead of `retro_config.json`
   - Need to update config loading to use retro config

4. **Ken Burns Default Setting**
   - Config has `ken_burns_movement: false` but code defaults to `true`
   - Need to align code with config file setting

5. **Shake Effect Implementation**
   - Currently using PIL-based shake_zoom effect
   - Need to replace with FFmpeg-based shake effect that oscillates the frame
   - Voice cutting between frames needs to be fixed

## Implementation Plan

### Step 1: Update Config Loading
- Change config file from `n8n_config.json` to `retro_config.json`
- Update default values to match retro_config.json
- Ensure ken_burns_movement defaults to false

### Step 2: Fix BGM Directory
- Change directory path from `assets/n8n_bgm` to `assets/retro_bgm`
- Ensure directory is created if it doesn't exist

### Step 3: Add Download Route
- Add `/download/<video_id>` route to serve completed videos
- Ensure proper file serving with correct MIME type

### Step 4: Replace Shake Effect Implementation
- Remove PIL-based shake_zoom effect
- Implement FFmpeg-based shake effect using video filters
- Fix voice cutting issue by using single audio source for all frames

### Step 5: Update Frame Processing
- Ensure shake effect is applied in both transition and non-transition paths
- Fix audio timing to prevent voice cutting between frames
- Use single audio file with proper time slicing

## File Changes Required

1. `simple_routes_fixed.py`:
   - Update config loading
   - Fix BGM directory path
   - Add download route
   - Update shake effect implementation

2. `retro_config.json`:
   - Verify settings are correct (ken_burns_movement: false, shake_zoom enabled)

## Testing Plan

1. Verify download route works correctly
2. Confirm BGM files are loaded from correct directory
3. Check that shake effect is applied to frames
4. Ensure no voice cutting occurs between frames
5. Validate config settings are properly loaded